{
    "name": "bcdocs",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "local": "vite --host",
        "dev": "vue-tsc -b && vite build --mode dev",
        "prod": "vue-tsc -b && vite build --mode prod",
        "build": "vue-tsc -b && vite build",
        "validate": "vue-tsc -b",
        "preview": "vite preview",
        "test": "cls && vitest",
        "coverage": "vitest run --coverage",
        "tests": "clear && vitest"
    },
    "dependencies": {
        "vue": "^3.5.13"
    },
    "devDependencies": {
        "@vitejs/plugin-vue": "^5.2.1",
        "typescript": "~5.6.2",
        "vite": "^6.0.1",
        "vue-tsc": "^2.1.10"
    }
}
